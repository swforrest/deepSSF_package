"""Model architecture: the deepSSF network definition.

Define your PyTorch model here, e.g.:

    import torch.nn as nn

    class DeepSSF(nn.Module):
        def __init__(self, ...):
            super().__init__()
            ...
        def forward(self, x):
            ...
"""
