"""Training: the fitting loop and anything needed to run it.

Put your training entry point here, e.g.:

    def train(model, dataloader, *, epochs, lr, ...):
        '''Fit the model and return it (plus training history).'''
        ...
"""
