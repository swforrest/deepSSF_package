"""Data preparation: turn raw tracking + environmental layers into model inputs.

This is where the Python-only data-prep functions you're porting from R /
the deepSSF_transfer repo should live, so the whole pipeline runs without R.

Drop your functions in below. A typical public entry point might look like:

    def prepare_data(tracking, layers, ...):
        '''Build the spatial/temporal tensors the model trains on.'''
        ...
"""
